import { ConstValue, FunctionType } from '@creditkarma/thrift-parser';
import { IResolveContext } from '../types';
export declare function resolveConstValue(value: ConstValue, expectedType: FunctionType, context: IResolveContext): ConstValue;
